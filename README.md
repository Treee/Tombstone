# Tombstone Source

## Setup

### Git Large File Storage

-   https://git-lfs.com/ Required for managing large blob files such as pngs

-   `git lfs install` To install

-   `git lfs track "*.psd"` to add new tracked files larger than 50mb

-   Make sure to keep `.gitattributes` tracked and up to date
